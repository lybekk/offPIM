/*
    Not in use
*/
const form = {
    state: {
        properties: {
            /*
            name:null,
            title:null,
            description:null
            */
        }
    },
    mutations: {
        /*
        setFormProperty (state, payload) {
            state.properties[payload.property] = payload.value
        },
        resetForm (state) {
            let v = state.properties;
            for (let key in v ) {
                v[key] = null;
              }
            //state.properties = {}
        }
        */
    },
    actions: {
    },
    getters: {
        /*
        getFormProperty: (state) => (property) => {
            if(state.properties.hasOwnProperty(property)){
                return state.properties[property]
            } else {
                //return null
                return ''
            }
        },
        */
    },
}

export default form